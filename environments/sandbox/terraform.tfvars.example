# Copy to terraform.tfvars and fill in values (never commit terraform.tfvars)

db_password          = "CHANGE_ME_strong_password"
ssh_allowed_cidrs    = ["YOUR_IP/32"]
web_ui_allowed_cidrs = ["0.0.0.0/0"]
